class CityModel{
  String id,name,city_id,zipcode_id,minimum_free_delivery_order_amount,delivery_charges;

  CityModel(this.id, this.name, this.city_id, this.zipcode_id,
      this.minimum_free_delivery_order_amount, this.delivery_charges);
}